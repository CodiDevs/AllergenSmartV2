[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: tests que tocan la base de datos real de Supabase (requieren DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning
